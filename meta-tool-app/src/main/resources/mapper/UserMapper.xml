<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeeiee.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yeeiee.entity.dto.UserDto">
        <id column="u_id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <collection property="roles" ofType="java.lang.String">
            <result column="role_name"/>
        </collection>
        <collection column="role_id" property="menus" select="com.yeeiee.mapper.MenuMapper.selectMenusByRoleId">
        </collection>
    </resultMap>

    <select id="selectByUserName" resultMap="BaseResultMap">
        select a.id as u_id,
               a.username,
               a.`password`,
               b.role_id,
               c.role_name
        from (select id,
                     username,
                     password
              from t_user
              where username = #{username}) a
                 join t_user_role b
                      on a.id = b.user_id
                 join t_role c
                      on b.role_id = c.id
    </select>

</mapper>
