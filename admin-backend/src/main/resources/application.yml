spring:
  profiles:
    active: dev

# 控制台打印sql
decorator:
  datasource:
    p6spy:
      # 日志格式
      log-format: "Execute SQL in %(executionTime)ms: [%(sql)]"

springdoc:
  swagger-ui:
    # 页面设置不展开
    doc-expansion: none
    # 关闭 swagger-ui
    # enabled: false