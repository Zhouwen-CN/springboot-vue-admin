spring:
  profiles:
    active: dev

# 控制台打印sql
decorator:
  datasource:
    p6spy:
      # 日志格式
      log-format: "Execute SQL in %(executionTime)ms: [%(sql)]"

springdoc:
  swagger-ui:
    # 关闭 swagger-ui，前端已经整合
    enabled: false